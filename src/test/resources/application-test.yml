server:
  shutdown: immediate


management.endpoint:
  health.cache.time-to-live: 0
  info.cache.time-to-live: 0

oauth:
  api.base.url: http://localhost:8090/auth

prison.api.base.url: http://localhost:8086
community.api.base.url: http://localhost:8087

offender.events:
  client:
    id: offender-events-client
    secret: clientsecret

application:
  events:
    poll:
      frequency: 1000
      initialDelay: 0
