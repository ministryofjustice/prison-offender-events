server:
  shutdown: immediate


management.endpoint:
  health.cache.time-to-live: 0
  info.cache.time-to-live: 0

oauth:
  api.base.url: http://localhost:8090/auth

prison.api.base.url: http://localhost:8086

offender.events:
  client:
    id: offender-events-client
    secret: clientsecret

sns.aws.access.key.id:
sns.aws.secret.access.key:
sns.topic.arn:
hmpps.sns.aws.access.key.id:
hmpps.sns.aws.secret.access.key:
hmpps.sns.topic.arn:
sns.provider:
